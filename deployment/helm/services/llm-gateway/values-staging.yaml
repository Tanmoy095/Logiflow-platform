# values-staging.yaml – overrides for staging environment
#DEPLOYMENT/HELM/SERVICES/llm-gateway/VALUES-STAGING.YAML

replicaCount: 2
image:
  tag: staging
  pullPolicy: Always
config:
  routingPolicy: "cost-optimized"
  modelTimeout: "45s"
